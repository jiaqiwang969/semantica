PREFIX chg: <https://product-trustworthiness.local/change#>
SELECT ?pass WHERE {
  chg:Change_0091 chg:changedDimension ?dim ; chg:changedFrom ?oldValue .
  ?pass a chg:PassRecord ; ?binding ?oldValue .
  ?binding chg:bindsDimension ?dim .
}
ORDER BY ?pass
